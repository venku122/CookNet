import React, { Component } from 'react';
import PropTypes from 'prop-types';
import Immutable from 'immutable';

class AddInstructionsInput extends Component {

  render() {
    return (
      <div>Add Instructions Input</div>
    );
  }
}

export default AddInstructionsInput;
