import React, { Component } from 'react';
import PropTypes from 'prop-types';
import Immutable from 'immutable';

class AddMetadataInput extends Component {

  render() {
    return (
      <div>Add Metadata Input</div>
    );
  }
}

export default AddMetadataInput;
